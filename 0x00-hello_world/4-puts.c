#include <stdio.h>

/**
 * main - entry which prints hello world
 * Return: is 0 which means success
 */

int main(void)
{

	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
